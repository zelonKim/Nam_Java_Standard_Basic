package Stream1;

import java.util.Arrays;
import java.util.Random;
import java.util.stream.*;

public class StreamEx1 {
	public static void main(String[] args) {
//		List<Integer> list = Arrays.asList(1,2,3,4,5);
//		Stream<Integer> MyStream1 = list.stream(); // 스트림 생성	// Stream<E> Collection.stream()
//		MyStream1.forEach( (i)->System.out.print(i*10) ); // 최종 연산  // 10 20 30 40 50

		
//		Stream<String> MyStream2 = Stream.of("a", "b", "c", "d"); // 스트림 생성  // Stream<T> Stream.of(T... values)
//		MyStream2.forEach( (i)->System.out.print(i) ); // 최종 연산  // a b c d

		
//		String[] strArr = {"a", "b", "c", "d"};
//		Stream<String> MyStream3 = Stream.of(strArr); // 스트림 생성  // Stream<T> Stream.of(T[])
//		MyStream3.forEach( (i)->System.out.print(i) ); // 최종 연산  // a b c d


//		Stream<String> MyStream4 = Arrays.stream(strArr); // 스트림 생성  // Stream<T> Arrays.stream(T[])
//		MyStream4.forEach( (i)->System.out.println(i) );  // 최종 연산  // a b c d
		
	
//		Stream<String> MyStream5 = Arrays.stream(strArr, 1, 3); // 스트림 생성  // Stream<T> Arrays.stream(T[], int start, int end)
//		MyStream5.forEach( (i)->System.out.println(i) );  // 최종 연산  // b c  

		
		
//		int[] intArr = {1, 2, 3, 4, 5};
		
//		IntStream MyStream6 = IntStream.of(intArr); // 스트림 생성  // IntStream IntStream.of(int[] arr)
//		MyStream6.forEach( (i)->System.out.println(i) );  // 최종 연산  // 1 2 3 4 5
		
//		IntStream MyStream7 = Arrays.stream(intArr); // 스트림 생성  // IntStream Arrays.stream(int[] arr)
//		MyStream7.forEach( (i)->System.out.println(i) );  // 최종 연산  // 1 2 3 4 5
		
	

	}
}





























